[project]
name = "etf-quant"
version = "0.1.0"
description = "ETF quantitative research platform"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "torch",
    "stable-baselines3",
]

[project.optional-dependencies]
dev = ["pytest"]
